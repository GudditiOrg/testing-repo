name: uploadfile-test
on: 
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Start Xvfb
        run: |
          sudo apt-get install -y xvfb
          Xvfb :99 -screen 0 1024x768x24 &
          export DISPLAY=:99

