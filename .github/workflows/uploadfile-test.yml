name: uploadfile-test
on: 
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Chrome
        uses: browser-actions/setup-chrome@v1
      
      - name: Run Chrome Headlessly
        run: google-chrome-stable --headless --version

